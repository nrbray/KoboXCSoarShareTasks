// 
// For internal operation, the config.xml and .stglobalignore files are shared.
// All files with .cup extension are shared. (Useful to share with FlySkyHigh)
// Files beginning with an upper case character are shared.
// Files beginning with a lower case character or a digit may be considered to be local to the device.
//
!/config.xml
!/.stglobalignore
!*.cup
![A-Z]*
**
/cert.pem
/csrftokens.txt
/https-cert.pem
/https-key.pem
/index-v0.14.0.db
/key.pem

